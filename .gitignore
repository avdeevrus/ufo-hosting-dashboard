# Окружение
.venv/
__pycache__/
*.pyc
.DS_Store

# Streamlit: коммитим config.toml (тема), но НЕ секреты
.streamlit/secrets.toml

# Тестовые артефакты
.playwright-mcp/
*.png

# Persistence Streamlit
*.log

# ВАЖНО: реальные данные клиентов не уходят в репозиторий.
# Каждый, кто клонирует репо, должен положить свои выгрузки локально.
data/orders/*.csv
data/ads/*.xlsx
reports/

# Сохраняем .gitkeep'ы чтобы структура папок была в репо
!data/orders/.gitkeep
!data/ads/.gitkeep
!reports/.gitkeep

# Папка проекта работы с клиентом (диалоги, доступы, стратегия) — репозиторий ПУБЛИЧНЫЙ, не коммитим
Проект_UFO_Hosting/
# Архив старых скриншотов/HAR и служебные worktree Claude
_archive/
.claude/worktrees/
